# 验证报告

## 状态定义

| 状态 | 结论 |
|---|---|
| `implemented` | 已实现 |
| `fixture_verified` | 已通过离线确定性数据验证 |
| `network_verified` | 已完成真实 CSI300 全量行情与扫描验证 |
| `pit_backtest_verified` | 仅离线 PIT fixture 已验证；真实历史 PIT 成分数据尚未导入 |

## 已执行验证

### 静态与离线测试

- `ruff check src tests scripts`：通过。
- `pytest`：82 项通过。
- `pytest --cov=stock_strategy_api`：通过，语句覆盖率 82%。
- `python -m compileall -q src tests`：通过。
- 独立目录验收：复制源码到 `/tmp/startkit-final-qa.EgojpO`，创建全新虚拟环境后可独立安装，基线测试与 Ruff 均通过；TestClient 成功读取独立构造的 fixture 推荐 `600000`。
- 范围审计：业务源码/测试中无旧项目绝对路径、`sys.path`、`quant_platform`、软链接或禁用机器学习依赖。
- 覆盖内容：股票代码、Parquet schema/原子写、交易日历、CSI300 有效期与快照质量、ST/退市/上市60日/北交所/停牌过滤、防未来数据、0.5% SHORT_GAP 边界、STRICT_GAP 子集、趋势/平台质量降级、D1五类承接、D2/D3配置化入场、4/5日退出、1～5日收益、MFE/MAE、旧版本隔离、SQLite幂等/事务回滚/历史状态查询、API默认过滤与错误契约、不可成交、T+1退出、成本和PIT门禁。

### v2 短线化真实数据与 API 验证

验证行情截至日：2026-08-10。

- CSI300 成分300只；raw/qfq共600个行情数据集全部更新至2026-08-10。顺序同步遇到大量间歇SSL/代理错误后，使用项目内进程隔离脚本增量补齐；最终日期审计缺失数为0。
- 新策略版本为 `2.0.0`。默认 SHORT_GAP 入口为0.5%缺口、1.5倍量比；原1%缺口、2倍量比及严格趋势平台口径作为 STRICT_GAP 子集标签。阶段分类仍携带 `fixed_window_phase_v0`，尚未冒充最终“同一上涨阶段”裁决。
- 默认补扫窗口按 `confirmation_days + max_entry_wait_days + 1` 计算为3个交易日，本次依次处理2026-08-06、08-07、08-10。08-06有1个历史D0候选，08-07和08-10均无新D0触发；08-10将历史候选推进出入场窗口。
- 启动真实Uvicorn后通过HTTP调用：`/readyz` 返回200，`expected_trade_date=latest_scan_date=2026-08-10`；默认 `/api/v1/recommendations` 返回 `total=0`、`stale=false`。因此截至08-10收盘没有仍具交易资格的推荐股票。
- 显式查询全部状态可见3条历史SHORT_GAP：中钨高新 `000657`（D0=08-06，D1完全未回补，原D2=08-10）、藏格矿业 `000408` 与北方稀土 `600111`（D0=08-05，原D2=08-07）；三者均为 `expired`，没有被默认API继续作为可买候选。
- 当前快照开发回测区间2026-07-01至07-28，run `02abbb1dc3fa4ea791d1652f8b46420f`：7个D0候选均取得D1交易资格并在D2成交；其中6个D1缺口完全未补、1个部分回补后收回，D2开盘前缺口破坏的候选不会虚构成交。1～5日、MFE/MAE、SHORT/STRICT、D1承接、阶段和入场延迟分组均成功输出。
- 上述回测使用 `current_snapshot`，`survivorship_bias=true`、证券主数据PIT覆盖率0、`production_verified=false`。7个样本只能证明代码和指标链路，不能据此认定0.5%/1.5倍、部分回补、4/5日退出或评分权重有效。

### v1 历史真实网络 smoke test

验证行情截至日：2026-08-07（当前会话日期 2026-08-10 可用数据的最近完整交易日）。

- 沪深交易日历：365 个自然日、242 个交易日，来源 `akshare_sina`。
- CSI300 当前成分：300 只，明确标记 `current_snapshot` 与 `survivorship_bias=true`。
- 沪深证券主数据：5,206 条；上海 2,311、深圳 2,895；上市日期缺失 0。
- 全量 raw/qfq 日线：300 只股票的 raw 与 qfq 共 600 个数据集全部更新到 2026-08-07，成功率 100%，raw/qfq 各 20,089 行。
- 全量扫描：300 只均完成资格与策略检查；资格排除 0、数据缺失 0、触发 0。299 只不满足实体缺口、300 只不满足 1% 缺口、292 只不满足 2 倍量比，其他硬条件失败数均已写入扫描 run `442c01c72c394109b37b23e87b800133`。
- 最近交易日补扫：默认窗口由策略的 `confirmation_days + max_entry_wait_days + 1` 动态计算，当前为 7 个交易日。按 2026-07-30 至 2026-08-07 顺序补扫并推进生命周期成功；即使 2026-08-03 至 2026-08-07 已有幂等运行也未跳过历史信号推进。七日 D0 触发数和最终确认数均为 0。
- 真实数据开发回测：2026-08-07 的 300 只股票逐日回放成功，run `2040457359504ebf89e879ea1e3af0ee`；当日候选和事件均为 0。由于股票池是当前快照，结果明确保留 `survivorship_bias=true`、`production_verified=false`。
- 真实 API 验证：启动 Uvicorn 后通过 HTTP 调用，`/readyz` 返回 200，预期交易日和最新扫描日均为 2026-08-07；默认及显式包含衰竭风险的推荐接口均返回新鲜的空结果，`meta.total=0`、`meta.stale=false`，证明“无候选”没有被误报为数据失败。

人工抽查最近两日日线：

| 股票 | 2026-08-06 | 2026-08-07 | 缺口结论 |
|---|---|---|---|
| 000001 平安银行 | high 11.28, close 11.27 | low 11.10, close 11.19 | `low <= previous high`；20 日量比 0.8133 |
| 600000 浦发银行 | high 9.35, close 9.29 | low 9.14, close 9.21 | 无向上缺口；20 日量比 0.7023 |
| 300750 宁德时代 | high 410.00, close 388.00 | low 384.02, close 388.07 | 无向上缺口；20 日量比 0.6392 |

量单位审计发现上游不同证券/端点的成交量单位不能只按交易所假定。生产采集代码现在通过 `amount / (close * volume)` 的批次中位关系识别“手/股”，并以数据源单位作为缺失成交额时的兜底。修复后 300 只 raw 的单位审计比值范围为 0.9930～1.0074，中位数 1.0001；qfq 为 0.9948～1.3129，中位数 1.0045。对应回归测试已加入。上表量比是同源 20 日窗口计算结果。

全量同步期间上游代理出现间歇 `SSLError`/`ProxyError`，首次请求失败的股票通过保留旧文件、进程隔离、超时和逐股票重试恢复；最终失败数据集为 0。过程中没有写入假数据或用 fixture 补齐。

## 尚未被证明的外部状态

- 尚未导入带历史有效日期的 CSI300 成分与证券状态，因此真实历史回测不能标记 `production_verified`。
- 网络端点偶发 SSL/代理断开；本次全量重试已恢复，但不能据此保证长期稳定性，生产同步仍会在任一未恢复数据集存在时明确失败。
